#ifndef STUDENT_VIEW_INPUT_REQUEST_MODELS_HPP
#define STUDENT_VIEW_INPUT_REQUEST_MODELS_HPP

#include <string>

struct student_view_assignments_input {
    // Currently the controller needs no data
    // activate the student view assignments use case
    // However I created this struct to allow for
    // future additions, like a time interval
};

#endif